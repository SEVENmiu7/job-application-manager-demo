# 求职投递管理器｜面试演示版

一个用于管理求职投递进度的全栈应用。项目提供看板、列表、岗位采集、进度更新和下一步安排等能力。

> 本仓库是面试演示副本，只包含虚构公司、虚构岗位和演示备注。它使用独立的飞书妙搭应用和数据库，不与个人正式投递数据共享记录。

## 核心功能

- 五阶段投递看板，支持拖动推进和重点置顶。
- 看板支持搜索重置、分列筛选和排序，并针对桌面宽度优化五列完整展示。
- 侧边栏支持手动折叠，收起后可悬停展开，兼顾大屏信息密度与导航效率。
- 投递列表、搜索、筛选、分页、状态快捷修改和材料详情抽屉。
- 工作地点支持多选、地区分组搜索和自定义录入，可记录跨城市或远程岗位。
- 岗位链接采集与结构化信息提取。
- 面试流程时间线，可通过统一的日历与时间面板查看、补录并编辑各阶段发生时间。
- 紧凑的阶段时间标签会根据当前主题展示已记录的流程节点。
- 补录流程节点时间时，系统会自动向前推进当前进度，同时避免结果状态意外回退。
- 面试复盘支持记录问题、回答要点、改进方向、团队信号和下一步行动。
- 支持浅色、深色、跟随系统，以及多套强调色切换。
- 投递时间按中国时区稳定显示，避免跨时区日期偏移。
- 当前进度、下一步安排和备注管理。
- 核心页面采用统一标题与说明；新增与编辑页面使用分段式信息结构，适合现场演示完整录入流程。
- 首次访问自动生成 15 条虚构演示数据。
- 用户之间的演示记录相互隔离。

## 技术栈

- React 19 + TypeScript + Vite
- NestJS 10
- PostgreSQL + Drizzle ORM
- Tailwind CSS + shadcn/ui
- 飞书妙搭 full-stack 运行时

## 目录结构

```text
client/     React 前端
server/     NestJS 后端和数据库迁移
shared/     前后端共享类型
scripts/    本地开发和质量检查脚本
test/       自动化测试
```

## 本地开发

环境要求：Node.js 22+ 和 npm 10+。

```bash
npm install
npm run dev
```

妙搭本地开发需要由平台下发 `.env.local`。仓库只提供不含密钥的 `.env.example`，不提供真实数据库连接信息。

## 演示数据

演示数据定义在 `server/modules/application/demo-applications.ts`。当当前用户没有任何投递记录时，后端会自动创建一组样例。删除全部记录后刷新页面，即可恢复初始演示数据。

## 隐私与公开仓库

仓库通过 `.gitignore` 排除 `.env`、`.env.local`、日志、依赖目录和构建产物。提交前建议运行：

```bash
git status
git diff --cached
git ls-files
```

请勿将真实简历、投递记录、Token、密码或数据库地址提交到公开仓库。

## 版本同步策略

正式版功能验证通过后，只同步前后端业务代码、通用配置和演示版所需的独立数据库结构。不同步 Git 历史、环境文件、正式版应用 ID、数据库记录或附件。

详细同步记录见 [`CHANGELOG.md`](CHANGELOG.md)。

## 许可说明

本仓库目前未声明开源许可证。代码公开用于作品展示和技术交流；如需复用或二次发布，请先取得作者许可。
